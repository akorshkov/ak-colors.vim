# ak_clolors vim/nvim colorscheme.

My own colorscheme based on 'torte'.

## Installation:

* With vim/pathogen:

```bash
git clone https://github.com/akorshkov/vim-ak-colors.git ~/.vim/bundle/vim-ak-colors
```

* With nvim Packer:

```lua
use { "akorshkov/vim-ak-colors" }
```
